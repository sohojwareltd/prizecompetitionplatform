/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // In Next.js 13+ with the /app directory, `appDir` is enabled by default.
  // The old `experimental.appDir` flag is no longer supported in Next 15,
  // so we remove it to avoid invalid config warnings/errors on build.
};

export default nextConfig;


